<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jwi.work.user.mapper.UserMapper">
        
    <insert id ="register" parameterType="com.jwi.work.dto.channel.createChannelDto">
    	INSERT INTO  channel (channelId,channelName,channelImageUrl,followerCount)value(#{channelId},#{channelName},#{channelImageUrl},#{followerCount})
    </insert>
    
    
</mapper>