<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jwi.work.mapper.SecurityMapper">
    
    
    
    
    
    
    <select id ="adminLoginCount" parameterType="String" resultType="int">
    	SELECT COUNT(*) WHERE adminId = #{adminId}
    </select>
    
    
    
    
      <select id="lodeAdminInfo"  parameterType="String" resultType="com.jwi.work.dto.adminDto.AdminDto">
        SELECT * FROM  WHERE adminId = #{adminId}
    </select>
    
    
    
    
   </mapper>